{
  "name": "glom",
  "description": "Fully offline Gaslands (https://gaslands.com/) vehicle squad manager.",
  "repository": {
    "type": "git",
    "url": "git://github.com/milogert/glom.git"
  },
  "private": true,
  "scripts": {
    "init": "npx elm-github-install",
    "start": "npx elm-app start",
    "build": "npx elm-app build",
    "deploy": "npx build && npx gh-pages -d build",
    "test": "npx elm-test",
    "coverage": "npx elm-coverage"
  },
  "dependencies": {
    "elm": "^0.19.0-bugfix6"
  },
  "devDependencies": {
    "elm-coverage": "^0.2.0",
    "elm-format": "^0.8.2",
    "elm-github-install": "^1.6.1",
    "elm-test": "^0.19.0-rev6",
    "gh-pages": "^2.1.1"
  }
}
